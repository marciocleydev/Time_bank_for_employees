server:
  port: 8080

spring:
  application:
    name: time_bank_for_employees
  datasource:
    url: jdbc:mysql://localhost:3306/time_bank_for_employees?useSSL=false&serverTimezone=UTC
    username: root
    password: 81050050

  jpa:
    hibernate:
     ddl-auto: none

    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: false
    open-in-view: false

logging:
   level:
    root: info #projeto completo.

    # Reduce ruído de outros módulos
    org.hibernate: ERROR
    org.springframework.transaction: ERROR